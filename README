Логи

Логи — это набор строк, где каждая строка представляет одну из возможных записей: метаданные следующей поисковой сессии, запрос, клик или переход на другую поисковую систему.

Строки про конкретную сессию идут подряд и отсортированы по времени. При- ведите код, который принимал бы на вход файл с логами, а на выходе выдавал бы файл с сессиями в виде последовательности действий вида sessionID QCCQC.

USAGE

java -classpath ./out/production/logs/ org.kslazarev.Main ./src/test/resources/example_file.log ./src/test/resources/actions_file.log
